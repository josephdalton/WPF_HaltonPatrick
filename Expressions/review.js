/**
 * Created by patrickhalton on 11/11/13.
 */


//count from 1 to 100 and (DONE)
// check to see if the number counted is divisible by 3. [use modulus?]
    // concatenate the values into a string.
//var myList='';
//for (x=1; x<=100; x++){
//if(x%3==0)
//myList = myList + x +', ';
//
//
//}
//console.log(x);

//day trader
//string of numbers count the number of
//increase or decrease

//create a random List of number
//house the numbers in an array
//loop to compare a number
//1-3


//var arrayList = [];
//for (x=0; x<20;x++){
//
//    arrayList[x]= Math.ceil(Math.random()*20);
//
//
//}
//console.log(arrayList);

//write and describe each piece of commands
//if
//for
//else
//if else
//expressions
//describe an array

//conditionals used to check values against each other
//if()
//use && and (and) || (or) for complex values
//uses ==,<=,>=,!= to chck values

//stuff done when things are checked and true


//for(loop that counts a number to certain length then perform an action)
//for(;;){}

//for(variableValue=1024);
//variableValue>10 //condition to be met
//variableValue = variableValue-100;
//A for loop is a programming construct which is used to repeat actions over and over again
//what is it doing over and over
//set the incrimentor in the value
//



var storageArray=[];
var storageString='';
var storgeCounter = 0;
for(i=0; i<20; i=i+1){

  storageArray[i]=Math.ceil(Math.random()*3);
if(i>0 && storageArray[i]<storageArray[i-1]> storageArray[i]);
storageString +=storageArray[i];
    storgeCounter++;{

    }
}








console.log(storageArray);
console.log(storageCounter);





















































